version: 0.2
phases:
  build:
    commands:
      - echo "Building the Docker image..."
      - docker build -t nginx:latest .
      - echo "Tagging the Docker image..."
      - docker tag nginx:latest 746256143333.dkr.ecr.ap-south-1.amazonaws.com/nginx:latest
      - echo "Pushing the Docker image to Amazon ECR..."
      - docker push 746256143333.dkr.ecr.ap-south-1.amazonaws.com/nginx:latest
